{
  "modid": "${ModID}",
  "name": "${project.name}",
  "author_list": [
    "iTitus"
  ],
  "credits": "Thanks to Ocean for generating a very helpful table to track implementation progress",
  "description": "${project.description}",
  "version": "${project.version}",
  "sts_version": "${SlayTheSpire.version}",
  "mts_version": "${ModTheSpire.version}",
  "dependencies": [
    "basemod",
    "stslib",
    "RelicStats",
    "downfall"
  ]
}
